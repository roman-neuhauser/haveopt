. haveopt.sh

tool()
{
  local ex=0 p="$channels" os="$optspecs"
  haveopt $p $os -- "$@" || ex=$?
  dump $p
  return $ex
}

dump()
{
  local var val
  for var; do
    eval val="\"\$$var\""
    printf "%s=%s\n" "$var" "$val"
  done
}
